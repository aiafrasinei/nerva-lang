fizzBuzz_f , n_i , _t ,
    res_t
    for i_i=1 i<=n i++
        s_s = ""
        if i % 3 == 0
            s.add "Fizz"
        if i % 5 == 0
            s.add "Buzz"
        if s.empty()
            s.add(i)
        
        res.add s
    ;
;
